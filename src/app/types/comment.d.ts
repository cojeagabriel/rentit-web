export interface Comment {
    _id?: string,
    _senderId: string,
    senderFirstName: string,
    senderLastName: string,
    _productId: string,
    comment: string,
    dateYear: number,
    dateMonth: number,
    dateDay: number,
}